extends workrecs/layout.pug

block title
    |{{ person.name }} - 勤怠編集
block menu
    li: a(href="{{ url_for('index')  }}") 一覧
block content
    form(method="POST")
        +generate_fields(form)
        input.btn.btn-success(type="submit",value="保存")
        a.btn.btn-danger(href="{{ url_for('workrecs.index', id=person.id, yymm=yymm) }}") キャンセル
 